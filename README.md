# 状态机模板库
Head-Only 状态机模板，提供两种状态机实现：

# ***原则：***
- 仲裁式的状态切换----***站在 整体视角***, 判断我应该处于哪个状态;
- 事件回调仅在【状态保持】时执行----***稳态环境 下***，跑事件回调;

# ***为用户提供：***
- 库级运行时防递归（直接抛异常）；
- 事件型状态机：同一实例拥有多线程安全的内容物副本，实例销毁即内存释放； 

# 状态机实现：

## 1. 刷新型状态机 ([basic_fsm/basic_fsm.h](basic_fsm/basic_fsm.h))

> ⚠️ **暂时弃用**：因并发性能问题，本 head-only 实现已暂停维护。后续将提供更合适的替代者 **OriginFsm**。

~~**核心特性**：~~
- ~~**仲裁式同步**：通过 [`resolver`](basic_fsm/basic_fsm.h:55) 函数决定下一个目标状态，与外部世界状态对齐~~
- ~~**状态行为**：每个状态可注册 enter / exit / hold 三种回调~~
- ~~**线程安全**：mutex + atomic，递归检测~~

~~**适用场景**：简单状态机、多层状态机~~

## 2. 事件型状态机 ([event_fsm/event_fsm.h](event_fsm/event_fsm.h))

通过事件触发状态转移，按键值 `(state, event)` 查找规则。

**核心特性**：
- **世界快照**：仅与当前这一刻的世界同步；Sync 时 resolver 拿到的是当前全部 pending 事件的完整快照，而非逐条处理
- **同一批次 = 同一时刻**：所有投递的事件视为同一时刻的扰动，resolver 需对此免疫
- **批量投递模式**：
  - **状态切换时**：整批事件直接丢弃，不执行任何 Action（取舍：放弃瞬时响应，换取一致性）
  - **状态保持时**：事件不去重【不分辨重复意图】；合法事件执行，非法事件丢弃
- **串行单发模式**：每次 Post 后立即 Sync，事件逐条仲裁
- **线程安全**：
  - 已移除行动锁（mutex），线程安全【串行化】由调用者保证
  - 采用并发容器管理线程本地内容物：同一实例在多线程下拥有独立副本，互不干扰
  - 实例销毁时自动释放全部关联内存，无泄漏
  - thread_local 实现 递归检测（触发时抛异常）

**适用场景**：中等复杂场景，性价比高

## 使用示例

见各个 FSM 文件内的注释示例：
- [basic_fsm 使用示例](basic_fsm/doc/basic_fsm_usage.md)
- [event_fsm 使用示例](event_fsm/doc/event_fsm_usage.md)
